# regular expression basics
- use regular expression library - https://regexlib.com/

- wild card on steroids
- used for data pattern matching

- data validation / extraction / 

-Match with $Matches
-Split: divide string based on delimiter
- replace: repalce every instance with provided
- select-string: 
- [regex]

# Symbols


# query group
- regex groups can be used to crate multiple groups to detect or manipulate data

# regex advanced
- mode modifieres can modify how the entire query behaves
- mode modifieres are encapsulated in ()

- regex namespace from .net can be used
